import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.apprestaurante.app',
  appName: 'App Restaurante',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
